[
  {
    "name": "PostgreSQL - Main Database",
    "type": "postgres",
    "description": "Main PostgreSQL database for N8N and application data storage",
    "data": {
      "host": "${POSTGRES_HOST:-postgres}",
      "port": "${POSTGRES_PORT:-5432}",
      "database": "${POSTGRES_DB:-n8n}",
      "username": "${POSTGRES_USER:-n8n_user}",
      "password": "${POSTGRES_PASSWORD}",
      "ssl": "disable"
    }
  },
  {
    "name": "Qdrant - Vector Database",
    "type": "httpHeaderAuth",
    "description": "Qdrant vector database for AI embeddings and similarity search",
    "data": {
      "name": "api-key",
      "value": "${QDRANT_API_KEY}"
    }
  },
  {
    "name": "Redis - Cache Database",
    "type": "redis", 
    "description": "Redis cache for session storage and temporary data",
    "data": {
      "host": "${REDIS_HOST:-redis}",
      "port": "${REDIS_PORT:-6379}",
      "password": "${REDIS_PASSWORD:-}",
      "database": "${REDIS_DATABASE:-0}"
    }
  },
  {
    "name": "OpenAI - API Service",
    "type": "httpHeaderAuth",
    "description": "OpenAI API for GPT models and embeddings generation",
    "data": {
      "name": "Authorization",
      "value": "Bearer ${OPENAI_API_KEY}"
    }
  },
  {
    "name": "Ollama - Local LLM Server",
    "type": "httpHeaderAuth",
    "description": "Ollama local LLM server for AI model inference",
    "data": {
      "name": "Authorization", 
      "value": "Bearer ollama-local"
    }
  },
  {
    "name": "Neo4j - Graph Database",
    "type": "neo4j",
    "description": "Neo4j graph database for knowledge graphs and relationship data",
    "data": {
      "scheme": "${NEO4J_SCHEME:-bolt}",
      "host": "${NEO4J_HOST:-neo4j}",
      "port": "${NEO4J_PORT:-7687}",
      "username": "${NEO4J_USER:-neo4j}",
      "password": "${NEO4J_PASSWORD}"
    }
  },
  {
    "name": "ClickHouse - Analytics Database",
    "type": "httpHeaderAuth",
    "description": "ClickHouse analytics database for time-series and analytics data",
    "data": {
      "name": "X-ClickHouse-User",
      "value": "${CLICKHOUSE_USER:-default}"
    }
  },
  {
    "name": "Document Processor API",
    "type": "httpHeaderAuth",
    "description": "Internal API for document processing and AI operations",
    "data": {
      "name": "Authorization",
      "value": "Bearer doc-processor-internal"
    }
  },
  {
    "name": "LightRAG API",
    "type": "httpHeaderAuth",
    "description": "Internal API for graph-based RAG operations",
    "data": {
      "name": "Authorization",
      "value": "Bearer lightrag-internal"
    }
  },
  {
    "name": "ETL Processor API", 
    "type": "httpHeaderAuth",
    "description": "Internal API for ETL data processing operations",
    "data": {
      "name": "Authorization",
      "value": "Bearer etl-processor-internal"
    }
  },
  {
    "name": "Web Interface API",
    "type": "httpHeaderAuth",
    "description": "Internal API for web interface management operations",
    "data": {
      "name": "Authorization",
      "value": "Bearer web-interface-internal"
    }
  },
  {
    "name": "Grafana - Monitoring Dashboard",
    "type": "httpBasicAuth",
    "description": "Grafana monitoring dashboard access",
    "data": {
      "user": "${GRAFANA_ADMIN_USER:-admin}",
      "password": "${GRAFANA_ADMIN_PASSWORD}"
    }
  },
  {
    "name": "Prometheus - Metrics Server",
    "type": "httpHeaderAuth",
    "description": "Prometheus metrics collection server",
    "data": {
      "name": "Authorization",
      "value": "Bearer prometheus-metrics"
    }
  }
]